czynność główna() {
    zm wejście_to_jeden: Czy = wczytaj() == 1
    jeśli(nie wejście_to_jeden)
        napisz(0)
    wpp
        napisz(1)

    jeśli(nie (nie wejście_to_jeden))
        napisz(1)
    wpp
        napisz(0)
}
